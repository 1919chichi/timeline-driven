---
description: 组件/模块文件级注释规约，新增 TSX/TS 文件必须遵循
globs: src/**/*.{ts,tsx}
alwaysApply: false
---

# 组件 / 模块注释规约

新增 / 重构 `src/` 下任意 `.ts` / `.tsx` 文件时，必须在文件顶部加一段 JSDoc 风格的中文注释，
说明该文件的「是什么 / 干什么 / 关键约束」。

## 1. 文件级头注释（必须）

放在所有 `import` 之前。模板：

```tsx
/**
 * <组件或模块名> —— <一句话说清职责>。
 *
 * - <主要行为/职责 1>
 * - <主要行为/职责 2>
 * - <重要约束、与其它模块的边界、数据流方向>
 */
import ...
```

对组件类文件还需说明：

- 该组件出现在哪个父组件 / 哪个状态分支（例如「TodayPage 的 ongoing 区域」）。
- 是受控还是自管理状态；如果是受控的，列出关键回调（onSave / toggleTask 等）。
- 与同目录其它相似组件的差异（例如 TaskItem vs UpcomingTaskItem）。

## 2. 关键函数与 props 注释

- 任何非平凡的导出函数 / hook / 回调，使用 `/** ... */` 简要说明语义，
  特别是涉及 **副作用、跨字段联动、边界条件** 的逻辑。
- Props interface 若有「非显而易见」的字段（可选、模式开关、回调副作用），逐字段加 `/** ... */`。
- 不要写复述代码的废话注释（如 `// 设置 state`）。注释只解释 **意图 / 约束 / 取舍**。

## 3. 示例

✅ 好的注释（解释意图与边界）：

```tsx
/**
 * SortableTaskItem —— 把 TaskItem 包成 @dnd-kit 可排序节点。
 * - 拖拽监听挂在外层 div，内部按钮 onClick 不受影响。
 * - 仅在 TodayPage 的 ongoing 区域使用。
 */
```

❌ 不合格（只复述类型或代码）：

```tsx
// 这是一个 React 组件
// 接收 props 并返回 JSX
```

## 4. 适用范围

- `src/components/**.tsx`：必须有文件级头注释。
- `src/hooks/**.ts`、`src/utils/**.ts`：必须有文件级头注释，并列出主要导出。
- `src/types.ts`：在文件顶部说明用途；新增字段需在字段上加 `/** ... */`。
- 单纯的入口/壳组件（如 `App.tsx`、`main.tsx`）也要写，便于后续接入路由时不漏改。

## 5. 校验提示

在 PR / 提交前自检：

- [ ] 新文件顶部存在 `/** ... */` 块？
- [ ] 块内是否说明「职责 / 关键约束 / 与谁交互」？
- [ ] 修改了 `Task` 字段或 `taskUtils` 完成判定？同步更新对应文件头注释。
